<Window x:Class="demo_win_gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:demo_win_gui"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 下拉选择框 -->
        <ComboBox x:Name="cmbOptions" Grid.Row="0" HorizontalAlignment="Left" Width="200"
                  Margin="0,0,0,20">
        </ComboBox>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="BtnPrint" Content="打印标签" Width="80" Margin="0,0,10,0" Click="BtnPrint_Click"/>
            <Button x:Name="BtnEdit" Content="编辑标签" Width="80" Click="BtnEdit_Click"/>
        </StackPanel>
        
    </Grid>
</Window>
